# Python 3.11+. No proprietary dependencies (CLAUDE.md conventions).
pandas>=2.0
statsmodels>=0.14
scipy>=1.11
matplotlib>=3.7
pytest>=7.4
openpyxl>=3.1
pydantic>=2          # Stream B/A coding schema (locator-or-raise)
pyyaml>=6            # one YAML per coded document
