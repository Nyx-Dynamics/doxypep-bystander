# Exact versions of the environment validated on 2026-08-21 (archival reference).
# Use this for exact reproduction; requirements.txt keeps broad bounds for normal use.
#
# Interpreter validated: CPython 3.12.12  (the code targets 3.11+; also run under 3.13)
# External tools (not pip): pandoc + a TeX Live pdflatex are required only for `make pdf`.
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
statsmodels==0.14.6
matplotlib==3.10.7
openpyxl==3.1.5
pydantic==2.12.4
PyYAML==6.0.3
pytest==9.1.1
