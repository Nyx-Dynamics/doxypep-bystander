"""Phase 0 feasibility gate (NOT YET IMPLEMENTED).

Computes the minimum detectable state-level change in tetracycline
non-susceptibility given: state population, MSM fraction, observed PrEP users,
an assumed doxy-PEP uptake bound (20%-55%), S. aureus isolate volume and the
plausible exposed fraction, and Soge's within-exposed RR 1.42. Emits a
sensitivity surface across the uptake bound.

Kill criterion (CLAUDE.md): if the required within-subgroup effect exceeds what
Soge observed, state-level ecological analysis cannot detect this -> pivot to
metro level. Write the negative feasibility result up either way.
"""
raise NotImplementedError("Phase 0 gate — implement before any Phase 1 code.")
